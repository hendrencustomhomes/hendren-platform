// Live worksheet routes now run through the orchestrated worksheet stack.
// Keep the legacy page available for rollback comparison until cleanup removes it deliberately.
export { default } from '@/components/patterns/pricing/PricingWorksheetPageOrchestrator'
