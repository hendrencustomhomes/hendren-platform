# Canonical Repo Tree

## Purpose

This file is the human-maintained structure source of truth for the Hendren Platform repo.

Whenever modules are added, moved, split, consolidated, or removed, this file must be updated.

## Status

Tree last updated: 2026-04-22 (Slice 1 pricing worksheet pattern split complete, build green)

## Current Tree (relevant excerpt)

```text
src/components/
├── pricing/
│   └── PricingWorksheetPage.tsx        (compat wrapper)
├── patterns/
│   └── pricing/
│       ├── PricingWorksheetPage.tsx
│       ├── PricingWorksheetHeader.tsx
│       ├── PricingWorksheetGrid.tsx
│       ├── PricingWorksheetMetaBar.tsx
│       ├── PricingWorksheetNewRowBar.tsx
│       └── PricingWorksheetMobileList.tsx
```

## Notes

- Pricing worksheet is now a shared pattern layer
- Old monolith replaced with compatibility wrapper
- Stitch file removed (no longer needed)
- Pattern is now consumed by both price sheets and bids

## Maintenance Requirement

If the repo structure changes, this file changes in the same pass.
